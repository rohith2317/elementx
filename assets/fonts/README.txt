Optional: drop a .ttf/.otf font here and load it in game/game.py's
_load_fonts() with pygame.font.Font("assets/fonts/yourfont.ttf", size)
instead of pygame.font.SysFont(...) for fully custom typography.
